<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - ARISTA BPN Sleman</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --deep-blue: #0C356A;
            --mid-blue: #0174BE;
            --gold: #FFC436;
            --cream: #FFF0CE;
            --white: #FFFFFF;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background: linear-gradient(135deg, var(--deep-blue) 0%, var(--mid-blue) 100%);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow: hidden;
        }

        .login-container {
            width: 100%;
            max-width: 450px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        /* Header Section */
        .login-header {
            text-align: center;
        }

        .login-logo {
            /* --- UKURAN CONTAINER --- */
            width: 120px;
            height: 120px;
            background-color: var(--cream);
            border-radius: 28px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
            border: 4px solid var(--gold);
            
            /* --- PERBAIKAN UTAMA: HAPUS PADDING --- */
            padding: 0; /* Menghapus jarak ke dalam agar gambar bisa penuh */
            overflow: hidden; /* Memastikan gambar tidak keluar dari border radius */
        }

        .login-logo img {
            /* --- MEMASTIKAN GAMBAR MENGISI PENUH --- */
            width: 100%;
            height: 100%;
            object-fit: contain; /* Gambar akan menyesuaikan diri tanpa gepeng */
            display: block;
        }

        .login-title {
            font-size: 32px;
            font-weight: 800;
            color: var(--gold);
            letter-spacing: -0.5px;
            margin-bottom: 5px;
            text-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .login-subtitle {
            font-size: 13px;
            color: var(--cream);
            font-weight: 500;
            opacity: 0.9;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Auth Card */
        .auth-wrap {
            background-color: var(--cream);
            border-radius: 30px;
            padding: 40px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            border-bottom: 8px solid var(--gold);
            position: relative;
        }

        .auth-wrap h2 {
            font-size: 26px;
            color: var(--deep-blue);
            margin-bottom: 30px;
            font-weight: 700;
            text-align: center;
        }

        /* Form Group */
        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 10px;
            font-size: 12px;
            font-weight: 800;
            color: var(--deep-blue);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .input-container {
            position: relative;
        }

        .input-container i {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--mid-blue);
            font-size: 18px;
        }

        .form-group input {
            width: 100%;
            padding: 14px 16px 14px 48px;
            border: 2px solid rgba(12, 53, 106, 0.1);
            border-radius: 14px;
            font-size: 15px;
            font-family: inherit;
            transition: all 0.3s ease;
            background-color: var(--white);
            color: var(--deep-blue);
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--mid-blue);
            box-shadow: 0 0 0 4px rgba(1, 116, 190, 0.1);
        }

        /* Button */
        .btn {
            width: 100%;
            padding: 16px;
            border: none;
            border-radius: 14px;
            font-size: 15px;
            font-weight: 800;
            cursor: pointer;
            transition: all 0.3s ease;
            background-color: var(--deep-blue);
            color: var(--cream);
            text-transform: uppercase;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            margin-top: 10px;
        }

        .btn:hover {
            background-color: var(--mid-blue);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(12, 53, 106, 0.3);
        }

        /* Footer Link */
        .register-link-container {
            text-align: center;
            margin-top: 25px;
            border-top: 1px solid rgba(12, 53, 106, 0.1);
            padding-top: 20px;
        }

        .register-link-container p {
            font-size: 14px;
            color: #555;
            font-weight: 500;
        }

        .small-link {
            color: var(--mid-blue);
            text-decoration: none;
            font-weight: 700;
        }

        .small-link:hover {
            color: var(--deep-blue);
            text-decoration: underline;
        }

        .login-footer {
            text-align: center;
            font-size: 12px;
            color: var(--cream);
            opacity: 0.8;
            font-weight: 500;
        }

        /* Responsive height adjustment */
        @media (max-height: 700px) {
            .auth-wrap {
                padding: 30px;
            }

            .form-group {
                margin-bottom: 15px;
            }

            .login-container {
                gap: 15px;
            }
        }
    </style>
</head>

<body>
    <div class="login-container">

        <header class="login-header">
            <div class="login-logo">
                <img src="/images/arista_logo.png" alt="Logo">
            </div>
            <h1 class="login-title">ARISTA</h1>
            <p class="login-subtitle">Arsip Digital Terintegrasi</p>
        </header>

        <main class="auth-wrap">
            <h2>Sign In</h2>

            <% if (typeof error !=='undefined' && error) { %>
                <div style="background: #fee2e2; color: #991b1b; padding: 12px; border-radius: 10px; margin-bottom: 20px; font-size: 14px; display: flex; align-items: center; gap: 10px; border-left: 4px solid #ef4444;">
                    <i class="fas fa-exclamation-circle"></i>
                    <%= error %>
                </div>
            <% } %>

            <form action="/login" method="post" novalidate>
                <div class="form-group">
                    <label>Username</label>
                    <div class="input-container">
                        <i class="fas fa-user-circle"></i>
                        <input name="username" type="text" placeholder="Masukkan username"
                            value="<%= (typeof old !== 'undefined' && old.username) ? old.username : '' %>" required>
                    </div>
                </div>

                <div class="form-group">
                    <label>Password</label>
                    <div class="input-container">
                        <i class="fas fa-key"></i>
                        <input name="password" type="password" placeholder="Masukkan password" required>
                    </div>
                </div>

                <button class="btn" type="submit">
                    Masuk Ke Sistem <i class="fas fa-arrow-right"></i>
                </button>
            </form>

            <div class="register-link-container">
                <p>Belum punya akses? <a class="small-link" href="/register">Hubungi Admin</a></p>
            </div>
        </main>

        <footer class="login-footer">
            <p>&copy; 2026 <strong>ARISTA BPN Sleman</strong></p>
        </footer>
    </div>
</body>

</html>